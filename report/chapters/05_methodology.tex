\section{Phương pháp thực hiện}
\label{sec:methodology}

Báo cáo so sánh ba hướng tiếp cận (pipeline) khác nhau cho bài toán trích xuất thông tin biên lai tiếng Việt: phương pháp Baseline dựa trên luật, mô hình sinh end-to-end Donut, và mô hình đa phương thức LayoutXLM kết hợp với VietOCR. Nội dung tiếp theo trình bày chi tiết thiết kế và quy trình hoạt động của từng pipeline.

\subsection{Tổng quan kiến trúc hệ thống}
\label{subsec:system_overview}

Hệ thống được cấu trúc theo dạng các module độc lập, cho phép kiểm thử và đánh giá từng kiến trúc một cách riêng biệt. Sơ đồ tổng quan của các pipeline được thể hiện trong \cref{fig:pipeline_overview}.
Đầu vào là ảnh biên lai, đầu ra là tệp JSON chứa bốn trường:
\field{store\_name}, \field{date}, \field{total}, \field{address}.

\begin{itemize}
  \item \textbf{Pipeline 1 — Baseline (Rule-based):}
    Ảnh $\rightarrow$ \gls{ocr} (PaddleOCR + VietOCR) $\rightarrow$
    Luật / Regex $\rightarrow$ JSON.

  \item \textbf{Pipeline 2 — \model{Donut} (OCR-free):}
    Ảnh $\rightarrow$ Swin Encoder $\rightarrow$ \model{mBART} Decoder
    $\rightarrow$ JSON.

  \item \textbf{Pipeline 3 — VietOCR + \model{LayoutXLM} (OCR-based):}
    Ảnh $\rightarrow$ \gls{ocr} $\rightarrow$ Token + BBox $\rightarrow$
    \model{LayoutXLM} $\rightarrow$ \gls{bio} $\rightarrow$ JSON.
\end{itemize}

\begin{figure}[htbp]
  \centering
  \resizebox{\textwidth}{!}{
    \begin{tikzpicture}[
      >=stealth,
      node distance=0.6cm and 0.8cm,
      input_style/.style={
        rectangle, draw=gray!60, fill=white, double,
        minimum width=2.2cm, minimum height=1.5cm, align=center,
        font=\small\bfseries, rounded corners=3pt
      },
      output_style/.style={
        rectangle, draw=huce-blue!80!black, fill=huce-blue!5,
        minimum width=2.5cm, minimum height=2.2cm, align=left,
        font=\small, rounded corners=6pt, line width=1pt
      },
      p1_style/.style={
        rectangle, draw=teal!60!black, fill=teal!10, text=teal!90!black,
        minimum width=2.4cm, minimum height=1.1cm, align=center,
        font=\small\bfseries, rounded corners=4pt
      },
      p1_sub/.style={
        rectangle, draw=teal!40!black, fill=teal!60, text=white,
        minimum width=2.2cm, minimum height=1.1cm, align=center,
        font=\small\bfseries, rounded corners=4pt
      },
      p1_final/.style={
        rectangle, draw=teal!40!black, fill=teal!80!black, text=white,
        minimum width=2.2cm, minimum height=1.1cm, align=center,
        font=\small\bfseries, rounded corners=4pt
      },
      p2_style/.style={
        rectangle, draw=orange!80!black, fill=orange!50, text=white,
        minimum width=2.8cm, minimum height=1.1cm, align=center,
        font=\small\bfseries, rounded corners=4pt
      },
      p2_dec/.style={
        rectangle, draw=orange!80!black, fill=orange!70, text=white,
        minimum width=2.8cm, minimum height=1.1cm, align=center,
        font=\small\bfseries, rounded corners=4pt
      },
      p3_style/.style={
        rectangle, draw=green!60!black, fill=green!10, text=green!80!black,
        minimum width=2.4cm, minimum height=1.1cm, align=center,
        font=\small\bfseries, rounded corners=4pt
      },
      p3_emb/.style={
        rectangle, draw=green!50!black, fill=green!40, text=white,
        minimum width=2.4cm, minimum height=1.1cm, align=center,
        font=\small\bfseries, rounded corners=4pt
      },
      p3_enc/.style={
        rectangle, draw=green!50!black, fill=green!60, text=white,
        minimum width=2.4cm, minimum height=1.1cm, align=center,
        font=\small\bfseries, rounded corners=4pt
      },
      p3_clf/.style={
        rectangle, draw=green!50!black, fill=green!80!black, text=white,
        minimum width=2.4cm, minimum height=1.1cm, align=center,
        font=\small\bfseries, rounded corners=4pt
      },
      group_p1/.style={
        rectangle, draw=teal!30, fill=teal!3,
        rounded corners=8pt, inner sep=12pt
      },
      group_p2/.style={
        rectangle, draw=orange!30, fill=orange!3,
        rounded corners=8pt, inner sep=12pt
      },
      group_p3/.style={
        rectangle, draw=green!30, fill=green!3,
        rounded corners=8pt, inner sep=12pt
      }
    ]

      \node[input_style] (input) at (0,0) {Ảnh biên lai\\đầu vào};

      \node[p2_style, right=2.0cm of input] (d1) {Swin Transformer\\Encoder};
      \node[p2_dec, right=1.2cm of d1] (d2) {mBART\\Decoder};

      \node[p1_style, above=2.5cm of d1.west, anchor=west] (b1) {OCR\\(PaddleOCR\\+ VietOCR)};
      \node[p1_sub, right=1.0cm of b1] (b2) {Văn bản thô};
      \node[p1_final, right=1.0cm of b2] (b3) {Trích xuất\\bằng Regex};

      \node[p3_style, below=2.5cm of d1.west, anchor=west] (l1) {OCR\\(PaddleOCR\\+ VietOCR)};
      \node[p3_emb, right=0.6cm of l1] (l2) {Nhúng đa\\phương thức\\(Text+Layout+Vis)};
      \node[p3_enc, right=0.6cm of l2] (l3) {LayoutXLM\\Encoder};
      \node[p3_clf, right=0.6cm of l3] (l4) {Phân loại Token\\(BIO labels)};

      \node[output_style, right=7.8cm of d2.west, anchor=west] (output) {
        \textbf{Chuỗi JSON}\\
        \textbf{có cấu trúc:}\\
        \ \ $\bullet$\ \field{store\_name}\\
        \ \ $\bullet$\ \field{date}\\
        \ \ $\bullet$\ \field{total}\\
        \ \ $\bullet$\ \field{address}
      };

      \begin{scope}[on background layer]
        \node[group_p1, fit=(b1) (b3), label={[font=\small\bfseries, text=teal!80!black]above:Pipeline 1: Baseline (OCR-based, Rule-based)}] (g1) {};
        \node[group_p2, fit=(d1) (d2), label={[font=\small\bfseries, text=orange!80!black]above:Pipeline 2: Donut (OCR-free, End-to-End)}] (g2) {};
        \node[group_p3, fit=(l1) (l4), label={[font=\small\bfseries, text=green!80!black]below:Pipeline 3: LayoutXLM (OCR-based, Multimodal)}] (g3) {};
      \end{scope}

      \draw[->, thick, draw=huce-blue] (input.east) -- ++(0.5,0) |- (b1.west);
      \draw[->, thick, draw=huce-blue] (input.east) -- (d1.west);
      \draw[->, thick, draw=huce-blue] (input.east) -- ++(0.5,0) |- (l1.west);

      \draw[->, thick] (b1) -- (b2);
      \draw[->, thick] (b2) -- (b3);

      \draw[->, thick] (d1) -- (d2);

      \draw[->, thick] (l1) -- (l2);
      \draw[->, thick] (l2) -- (l3);
      \draw[->, thick] (l3) -- (l4);

      \draw[->, thick, draw=huce-blue] (b3.east) -| ([xshift=-0.5cm]output.west) |- ([yshift=0.4cm]output.west);
      \draw[->, thick, draw=huce-blue] (d2.east) -- (output.west);
      \draw[->, thick, draw=huce-blue] (l4.east) -| ([xshift=-0.5cm]output.west) |- ([yshift=-0.4cm]output.west);

    \end{tikzpicture}
  }
  \caption{Sơ đồ tổng quan kiến trúc ba pipeline trích xuất thông tin biên lai.}
  \label{fig:pipeline_overview}
\end{figure}


\subsection{Baseline: OCR + Trích xuất dựa trên luật (Rule-based)}
\label{subsec:baseline}

Baseline hoạt động như một hệ chuyên gia đơn giản, dựa trên giả định rằng các trường thông tin quan trọng thường nằm gần các từ khóa đặc trưng hoặc tuân theo một quy luật nhất định trên biên lai.
Phương pháp dùng các quy tắc thủ công (heuristic) và
biểu thức chính quy (regular expression — regex) để trích xuất thông tin từ
kết quả \gls{ocr}. Đây là phương pháp đơn giản nhất, không cần huấn luyện
mô hình học sâu, nhưng phụ thuộc nhiều vào chất lượng \gls{ocr} và sự nhất
quán của bố cục biên lai.

\subsubsection{Pipeline tổng quan}
\label{subsubsec:baseline_pipeline}

Quy trình xử lý gồm hai bước chính:

\begin{enumerate}
  \item \textbf{Nhận dạng văn bản:} Ảnh biên lai được đưa qua bộ phát hiện
    vùng chữ PaddleOCR \cite{du2020ppocr} và bộ nhận dạng VietOCR
    \cite{vietocr2020} để thu được danh sách các cặp (văn bản, bounding box)
    sắp xếp theo thứ tự đọc (reading order).

  \item \textbf{Trích xuất theo luật:} Áp dụng các quy tắc heuristic và regex
    riêng cho từng trường thông tin để trích xuất giá trị từ danh sách văn bản.
\end{enumerate}

\subsubsection{Quy tắc trích xuất cụ thể}
\label{subsubsec:baseline_rules}

Cách trích xuất cho từng trường như sau:
\begin{itemize}
  \item \textbf{Tên cửa hàng (\field{store\_name}):} Chấm điểm tối đa 5 dòng đầu theo vị trí, tỷ lệ chữ hoa và độ dài. Các dòng chứa từ khóa hóa đơn, mã số thuế, điện thoại hoặc địa chỉ, dòng quá ngắn và dòng có tỷ lệ chữ số cao bị loại.
  \item \textbf{Ngày tháng (\field{date}):} Duyệt từ trên xuống, chuẩn hóa từng dòng và lấy ngày hợp lệ đầu tiên trong khoảng năm 1990--2040 theo hằng số \code{VALID\_YEAR\_RANGE}. Bộ chuẩn hóa hỗ trợ dấu \code{/}, \code{-}, \code{.} và năm hai chữ số.
  \item \textbf{Tổng tiền (\field{total}):} Duyệt từ cuối hóa đơn lên để ưu tiên từ khóa tổng tiền gần phần thanh toán. Với mỗi dòng có từ khóa, xét dòng đó và tối đa một dòng kế tiếp, lọc số điện thoại, mã số thuế, ngày và giá trị dưới 1.000 rồi chọn số tiền lớn nhất. Nếu không có từ khóa, thuật toán tìm ứng viên hợp lệ trong một phần ba cuối hóa đơn.
  \item \textbf{Địa chỉ (\field{address}):} Tìm dòng có từ khóa địa chỉ nhưng không chứa ngày, điện thoại, mã số thuế hoặc từ khóa thanh toán. Nếu các dòng kế tiếp không bị chặn và có nội dung đủ dài, nối thêm tối đa ba dòng kế tiếp trước khi chuẩn hóa.
\end{itemize}
Các quy tắc trên được xây dựng dựa trên quan sát thực tế trên tập
dữ liệu biên lai tiếng Việt. \Cref{tab:baseline_rules} tóm tắt các quy tắc
chính của từng trường.

\begin{table}[htbp]
  \centering
  \caption{Quy tắc trích xuất cho từng trường thông tin trong pipeline baseline.}
  \label{tab:baseline_rules}
  \begin{tabularx}{\textwidth}{@{} l X @{}}
    \toprule
    \textbf{Trường} & \textbf{Mô tả quy tắc} \\
    \midrule
    \field{store\_name}
      & Chấm điểm tối đa 5 dòng đầu; loại các dòng giống tiêu đề hóa đơn,
        mã số thuế, điện thoại, địa chỉ hoặc chứa quá nhiều chữ số. \\
    \addlinespace
    \field{date}
      & Tìm chuỗi khớp regex ngày tháng, bỏ qua các từ khoá:
        \code{ngày}, \code{date}, \code{time}, và chỉ giữ năm trong khoảng 1990--2040. \\
    \addlinespace
    \field{total}
      & Duyệt từ cuối lên, xét dòng có từ khóa và một dòng kế tiếp; nếu
        không có từ khóa thì tìm số tiền hợp lệ trong một phần ba cuối. \\
    \addlinespace
    \field{address}
      & Tìm dòng có từ khóa địa chỉ, loại ngữ cảnh bị chặn và nối tối đa
        ba dòng kế tiếp hợp lệ. \\
    \bottomrule
  \end{tabularx}
\end{table}

Các mẫu regex dùng cho trường \field{date} và \field{total} được
trình bày trong \cref{lst:baseline_regex}.

\begin{lstlisting}[
  style=python,
  caption={Các mẫu regex dùng trong pipeline baseline.},
  label={lst:baseline_regex}
]
# --- Regex cho truong date ---
DATE_PATTERNS = [
    r'(\d{1,2})[/-](\d{1,2})[/-](\d{4})',   # dd/mm/yyyy hoac dd-mm-yyyy
    r'(\d{1,2})[/-](\d{1,2})[/-](\d{2})',    # dd/mm/yy
    r'(\d{4})[/-](\d{1,2})[/-](\d{1,2})',    # yyyy/mm/dd (ISO)
]
DATE_IGNORE_KW = ['ngay', 'date', 'time']

# --- Regex cho truong total ---
TOTAL_KEYWORDS = ['tong cong', 'thanh toan', 'total', 'phai tra']
MONEY_PATTERN  = r'\d[\d\.,\s]*'   # so tien: 150.000, 1,500,000
TOTAL_SEARCH_RANGE = 1              # dong keyword va mot dong ke tiep
\end{lstlisting}


\subsection{Donut: End-to-End OCR-free}
\label{subsec:donut_methodology}


Donut được chọn để kiểm tra xem liệu một mạng nơ-ron có thể tự học cách ``đọc'' và ``hiểu'' bố cục biên lai trực tiếp từ pixel, không cần bước OCR riêng. Nếu làm được, hệ thống sẽ không còn phụ thuộc vào chất lượng bộ OCR ngoài. Chi tiết kiến trúc Swin Encoder + mBART Decoder đã được trình bày ở \cref{subsec:donut}.


\subsubsection{Chuẩn bị dữ liệu}
\label{subsubsec:donut_data}

Chuỗi đích dùng cấu trúc thẻ kiểu XML để biểu diễn nhãn và quan hệ phân cấp của từng trường. Với cách này, BART Decoder có thể học sinh ra các thẻ đóng/mở cùng nội dung văn bản tương ứng mà không cần thêm cấu trúc biểu diễn phức tạp.
Mỗi mẫu huấn luyện là một cặp (ảnh biên lai, chuỗi đích). Chuỗi đích được
định dạng bằng các token đặc biệt đánh dấu từng trường thông tin. Ảnh đầu vào
được resize về kích thước $1280 \times 960$ pixel, giữ nguyên tỷ lệ khung hình
(aspect ratio) bằng cách thêm padding.

Ví dụ chuỗi đích cho một biên lai:

\begin{lstlisting}[
  style=yaml,
  caption={Ví dụ chuỗi đích (target string) cho mô hình \model{Donut}.},
  label={lst:donut_target}
]
<s_receipt_ie>
  <s_store_name>CUA HANG TIEN LOI GS25</s_store_name>
  <s_date>15/03/2024</s_date>
  <s_total>125.000</s_total>
  <s_address>123 Nguyen Trai, Thanh Xuan, Ha Noi</s_address>
</s_receipt_ie>
\end{lstlisting}

\subsubsection{Cấu hình huấn luyện}
\label{subsubsec:donut_training}

Cấu hình siêu tham số huấn luyện được thiết lập tương thích với tài nguyên GPU Tesla T4 16GB. Cụ thể, batch size thực tế được đặt là 2 kết hợp với tích lũy gradient 8 bước nhằm đạt batch size hiệu dụng bằng 16. Thiết lập này chủ yếu dựa trên giới hạn phần cứng sẵn có và chưa trải qua các lượt tối ưu hóa tham số nâng cao.
\Cref{tab:donut_config} liệt kê cấu hình huấn luyện của mô hình
\model{Donut}.

\begin{table}[htbp]
  \centering
  \caption{Cấu hình huấn luyện mô hình \model{Donut}.}
  \label{tab:donut_config}
  \begin{tabular}{@{} l l @{}}
    \toprule
    \textbf{Tham số} & \textbf{Giá trị} \\
    \midrule
    Pretrained model         & \code{naver-clova-ix/donut-base} \\
    Max length               & 768 tokens \\
    Epochs                   & 150 \\
    Batch size               & 2 \\
    Gradient accumulation    & 8 (effective batch size = 16) \\
    Learning rate            & $2 \times 10^{-5}$ \\
    LR scheduler             & Cosine \\
    Warmup ratio             & 0.1 \\
    FP16                     & Có \\
    Gradient checkpointing   & Có \\
    Early stopping patience  & 15 epochs \\
    \bottomrule
  \end{tabular}
\end{table}

\subsubsection{Suy luận (Inference)}
\label{subsubsec:donut_inference}

Khi suy luận, Donut nhận ảnh biên lai cùng prompt \token{s\_receipt\_ie}, sau đó decoder sinh tuần tự các token tiếp theo. Bộ parser tìm nội dung nằm giữa các cặp token mở/đóng; trường nào không tạo được cặp token hợp lệ sẽ được gán chuỗi rỗng trước khi chuẩn hóa.
Cụ thể, mô hình \model{Donut} nhận ảnh biên lai làm đầu vào
và sinh chuỗi văn bản qua beam search với \code{num\_beams=2}. Chuỗi đầu
ra được parse để lấy giá trị của từng trường
rồi chuyển thành định dạng JSON.

Quy trình suy luận:
\begin{enumerate}
  \item Ảnh biên lai được resize về $1280 \times 960$ và đưa qua encoder.
  \item Decoder sinh chuỗi đầu ra sử dụng beam search
    (\code{num\_beams=2}).
  \item Chuỗi đầu ra được parse bằng regex để trích xuất nội dung giữa
    các cặp token \token{s\_field} và \token{/s\_field}.
  \item Kết quả được ghi vào tệp JSON.
\end{enumerate}

\begin{warnbox}[Trạng thái artifact Donut]
Checkpoint Donut hiện tại không lưu \code{max\_length} trong \code{generation\_config.json}; vì vậy manifest chỉ ghi fallback metadata là 20 token trong trường hợp không override. Lượt prediction được dùng trong báo cáo hiện tại có sidecar/code cho thấy đã truyền explicit \code{generation\_max\_length=768} vào \code{generate()}. Do đó, kết quả Donut phản ánh hiện tượng sinh lặp và thiếu cặp thẻ hợp lệ của checkpoint hiện có, thay vì được diễn giải như bằng chứng rằng inference thực tế bị giới hạn ở 20 token.
\end{warnbox}


\subsection{VietOCR + LayoutXLM: OCR-based Layout-aware}
\label{subsec:layoutxlm_methodology}


Hướng tiếp cận này kết hợp VietOCR và \model{LayoutXLM}: VietOCR nhận dạng chữ viết tiếng Việt có dấu, LayoutXLM tích hợp văn bản với thông tin không gian (tọa độ bounding box) và ảnh để phân loại thực thể theo lược đồ \gls{bio}. Chi tiết kiến trúc đa phương thức của LayoutXLM đã được trình bày ở \cref{subsec:layoutxlm}.


\subsubsection{Chuẩn bị dữ liệu}
\label{subsubsec:layoutxlm_data}

LayoutXLM chỉ dùng các mẫu có \code{annotation\_level=json\_and\_boxes}. Với mỗi từ OCR, mã nguồn tính tỷ lệ phần diện tích của hộp từ nằm trong hộp ground-truth:
\begin{equation}
  \operatorname{coverage}(W,F)=\frac{|W\cap F|}{|W|}.
  \label{eq:word_coverage}
\end{equation}
Đây là tỷ lệ bao phủ hộp từ, không phải Intersection over Union. Từ được gán vào trường có coverage cao nhất nếu tỷ lệ đạt ít nhất 0,5; các từ còn lại nhận nhãn \code{O}.
\textbf{Đầu vào:} Kết quả \gls{ocr} từ cache bao gồm văn bản và bounding box
cho từng từ (word-level).

\textbf{Gán nhãn \gls{bio}:} Các từ liên tiếp cùng thuộc một trường nhận lần lượt nhãn \code{B-FIELD} và \code{I-FIELD}. Khi một từ không đạt ngưỡng coverage, chuỗi thực thể hiện tại kết thúc.

\textbf{Tokenization:} Văn bản được tokenize với giới hạn 512 token. Tham số \code{doc\_stride} được đặt là 128 trong cấu hình, nhưng dataset và inference hiện chưa tạo sliding windows. Chuỗi dài bị cắt ở giới hạn token, nên các trường nằm cuối biên lai có thể bị mất.

\begin{table}[htbp]
  \centering
  \caption{Ví dụ tối giản về gán nhãn \gls{bio} cho một chuỗi OCR.}
  \label{tab:bio-tagging-example-method}
  \begin{tabular}{ll}
    \toprule
    \textbf{Từ OCR} & \textbf{Nhãn BIO} \\
    \midrule
    Co.opmart & \code{B-STORE\_NAME} \\
    Đống & \code{I-STORE\_NAME} \\
    Đa & \code{I-STORE\_NAME} \\
    Ngày: & \code{O} \\
    16/05/2024 & \code{B-DATE} \\
    TỔNG CỘNG & \code{O} \\
    245.000 & \code{B-TOTAL} \\
    Địa chỉ: & \code{O} \\
    Hà & \code{B-ADDRESS} \\
    Nội & \code{I-ADDRESS} \\
    \bottomrule
  \end{tabular}
\end{table}

\subsubsection{Cấu hình huấn luyện}
\label{subsubsec:layoutxlm_training}

Siêu tham số lấy từ cấu hình thực nghiệm trong repository. Số epoch tối đa là 20 và early stopping patience là 5. Bảng dưới đây chỉ mô tả cấu hình khai báo; bằng chứng về số bước và epoch thực chạy được tổng hợp riêng trong \cref{subsec:training_evidence}.
\Cref{tab:layoutxlm_config} liệt kê cấu hình huấn luyện của mô hình
\model{LayoutXLM}.

\begin{table}[htbp]
  \centering
  \caption{Cấu hình huấn luyện mô hình \model{LayoutXLM}.}
  \label{tab:layoutxlm_config}
  \begin{tabular}{@{} l l @{}}
    \toprule
    \textbf{Tham số} & \textbf{Giá trị} \\
    \midrule
    Pretrained model         & \code{microsoft/layoutxlm-base} \\
    Max length               & 512 tokens \\
    Doc stride               & 128 (đã khai báo, chưa dùng sliding window) \\
    Số nhãn (labels)         & 9 nhãn \gls{bio} \\
    Epochs                   & 20 \\
    Batch size               & 8 \\
    Gradient accumulation    & 4 (effective batch size = 32) \\
    Learning rate            & $2 \times 10^{-5}$ \\
    LR scheduler             & Linear \\
    Warmup ratio             & 0.1 \\
    FP16                     & Có \\
    Early stopping patience  & 5 epochs \\
    \bottomrule
  \end{tabular}
\end{table}

Hệ thống nhãn \gls{bio} gồm 9 nhãn được mô tả chi tiết trong \cref{tab:bio_labels} ở \cref{sec:theory}.

Tập train LayoutXLM có 784 mẫu và validation có 168 mẫu có hộp bao. Các mẫu \code{json\_only} không tham gia được vào huấn luyện token classification. Trong khi đó, Donut không cần hộp bao nên dùng toàn bộ 1.091 mẫu train và 234 mẫu validation.

\subsubsection{Suy luận và hậu xử lý (Inference \& Post-processing)}
\label{subsubsec:layoutxlm_inference}

Thuật toán gộp nhãn hoạt động theo kiểu máy trạng thái: \code{B-FIELD} mở span mới, \code{I-FIELD} cùng loại thì nối vào span đang mở. Nếu gặp \code{I-FIELD} mà không có span cùng loại đang mở, triển khai hiện tại vẫn mở span mới để không mất thực thể — tương đương chính sách sửa chuỗi BIO bị đứt. Nhãn \code{O} hoặc nhãn trường khác sẽ đóng span hiện tại.
Quy trình suy luận của pipeline \model{LayoutXLM}:

\begin{enumerate}
  \item \textbf{Dự đoán nhãn \gls{bio}:} Mô hình \model{LayoutXLM} nhận
    đầu vào gồm token, bounding box và ảnh, sau đó dự đoán nhãn \gls{bio}
    cho từng token.

  \item \textbf{Gộp span và chọn ứng viên:} Sau khi tạo span, tên cửa hàng lấy span đầu tiên; ngày lấy span đầu tiên chuẩn hóa hợp lệ; tổng tiền loại số giống điện thoại/MST hoặc quá nhỏ rồi chọn giá trị lớn nhất; các span địa chỉ gần nhau theo trục $y$ được ghép thành block và ưu tiên block có từ khóa địa chỉ hoặc có độ dài lớn nhất.

  \item \textbf{Chuẩn hoá:} Văn bản trích xuất được chuẩn hoá (loại bỏ
    khoảng trắng thừa, chuẩn hoá Unicode \gls{nfc}) trước khi ghi vào
    tệp JSON đầu ra.
\end{enumerate}


\subsection{Hậu xử lý chung (Post-processing)}
\label{subsec:postprocessing}

Hậu xử lý chung có nhiệm vụ chuẩn hóa schema và một số định dạng trường trước khi đánh giá.
Kết quả đầu ra của cả ba pipeline đều đi qua bước hậu xử lý này trước khi so sánh với ground-truth, để đảm bảo nhất quán khi đánh giá.
Cụ thể gồm:

\begin{itemize}
  \item \textbf{Chuẩn hoá Unicode:} Chuyển đổi toàn bộ văn bản về dạng
    \gls{nfc} để thống nhất cách biểu diễn ký tự tiếng Việt.

  \item \textbf{Chuẩn hoá khoảng trắng:} Loại bỏ khoảng trắng đầu/cuối
    và thay thế các chuỗi khoảng trắng liên tiếp bằng một khoảng trắng đơn.

  \item \textbf{Định dạng thống nhất:} Đảm bảo tất cả các trường đầu ra
    đều có mặt trong tệp JSON, sử dụng chuỗi rỗng cho các trường không
    trích xuất được.
\end{itemize}

Triển khai hiện tại không chuyển \field{store\_name} và \field{address} sang chữ thường; EM vì vậy phân biệt hoa/thường. \field{date} được parse về ISO và \field{total} chỉ giữ chữ số.
